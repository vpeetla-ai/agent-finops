# API auth — unset = open (dev/demo). Set before treating a deployment as production.
AGENTFINOPS_API_KEY=

# Persistence backend: sqlite (dev default, in-memory) or postgres (production)
AGENTFINOPS_DB_BACKEND=sqlite
# Required when AGENTFINOPS_DB_BACKEND=postgres
DATABASE_URL=
